create a component that prevent children if width is small, which detects container width

add environment validation when building


update nextjs to version 16 when codegen support graphql version 17, cause as/next-integration works with nextjs 16 only supports latest graphql version, which breaks codegen