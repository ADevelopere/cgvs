"Response after successful login"
type AuthPayload {
    "The JWT token"
    token: String!
    "The authenticated user"
    user: User!
}

"Response after logout"
type LogoutResponse {
    "Status message"
    message: String!
}
